//! `chorus channel del <name>` — stub; HTTP logic lands in a later task.

pub async fn run(_name: String, _yes: bool, _server_url: &str) -> anyhow::Result<()> {
    anyhow::bail!("not yet implemented")
}
